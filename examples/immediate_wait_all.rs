#![deny(warnings)]
extern crate mpi;

// use mpi::traits::*;
// use mpi::request::{CancelGuard, WaitGuard};

fn main() {
    // let universe = mpi::initialize().unwrap();
    // let world = universe.world();


}
